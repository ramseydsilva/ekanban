define([
    'underscore',
    'backbone',
    'backbone-relational'
], function(_, Backbone, MaterialView) {
    var MaterialModel = Backbone.RelationalModel.extend({

    });

    return MaterialModel;

});
